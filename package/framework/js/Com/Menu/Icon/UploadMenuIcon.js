import MenuIcon from "../../../Menu/MenuIcon.js";

export default class UploadMenuIcon extends MenuIcon {

    constructor(parentContainer = null) {
        super(parentContainer);
        this.icon = "upload";
        this.label = "Upload";
    }

}